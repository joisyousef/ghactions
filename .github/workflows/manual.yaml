on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Choose the environment to deploy to"
        required: true
        default: 'Staging'
        type: choice
        options:
        - Staging
        - Production

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy Application
      run: echo "Deploying application manually triggered by user."
